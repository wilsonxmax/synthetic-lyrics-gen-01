# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# DiffSinger specific
praat-parselmouth>=0.4.3  # For phoneme extraction
pypinyin  # For Chinese phoneme support (if needed)
g2p-en  # For English phoneme conversion
pydub  # Audio processing
resampy  # Audio resampling

# Inference optimization (optional)
onnxruntime  # If using ONNX models
